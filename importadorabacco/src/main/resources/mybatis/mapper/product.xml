<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.importadorabacco.web.dao.ProductDao">

    <resultMap type="com.importadorabacco.web.model.Product" id="productResultMap">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="imageUrl" column="image_url" />
        <result property="catId" column="cat_id" />
        <result property="catName" column="cat_name" />
        <result property="price" column="price" />
        <result property="desc" column="desc" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <select id="selectById"  resultMap="productResultMap" parameterType="java.lang.Long">
        select * from product where id = #{id}
    </select>

    <select id="select"  resultMap="productResultMap" parameterType="java.util.Map">
        select * from product
        <where>
            <if test="catId != null">
                and cat_id = #{catId}
            </if>
        </where>
    </select>

    <select id="selectAllCatIds"  resultType="int" parameterType="java.util.Map">
        select DISTINCT cat_id from product
    </select>
</mapper>
