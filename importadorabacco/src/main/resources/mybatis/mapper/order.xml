<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.importadorabacco.web.dao.OrderDao">

    <resultMap type="com.importadorabacco.web.model.Order" id="orderResultMap">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="name" column="name" />
        <result property="mobile" column="mobile" />
        <result property="email" column="email" />
        <result property="address1" column="address1" />
        <result property="address2" column="address2" />
        <result property="city" column="city" />
        <result property="zipCode" column="zip_code" />
        <result property="createTime" column="create_time" />
    </resultMap>

    <insert id="insert" keyProperty="id" useGeneratedKeys="true" parameterType="com.importadorabacco.web.model.Order">
        insert into user_order (
        user_id,
        name,
        mobile,
        email,
        address1,
        address2,
        city,
        zip_code,
        create_time
        ) values (
        #{userId},
        #{name},
        #{mobile},
        #{email},
        #{address1},
        #{address2},
        #{city},
        #{zipCode},
        #{createTime}
        )
    </insert>

    <select id="selectById"  resultMap="orderResultMap" parameterType="java.lang.Long">
        select * from user_order where id = #{id} ;
    </select>

    <select id="queryUserLastOrder"  resultMap="orderResultMap" parameterType="java.lang.Long">
        select * from user_order where user_id = #{userId} order by create_time desc limit 1 ;
    </select>
</mapper>
